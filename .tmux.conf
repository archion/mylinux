set -g mode-mouse on
set -g mouse-select-pane on
set -g mouse-select-window on
set -g mouse-resize-pane on
set -g history-limit 10000
new -s work -n rtorrent 'rm -f Downloads/torrents/session/rtorrent.lock && rtorrent'
neww -n irssi 'irssi'
neww -n ssh 'ssh -X YZD@114.212.205.227 -p 2222'
splitw -h -c ~/Projects/FortranProgram/data
neww htop
neww
setw -g mode-keys vi
setw -g aggressive-resize on
bind-key z kill-session
bind-key C-b last-window
bind k selectp -U
bind j selectp -D
bind h selectp -L
bind l selectp -R
bind -r H resize-pane -L 5
bind -r J resize-pane -D 5
bind -r K resize-pane -U 5
bind -r L resize-pane -R 5
bind \ splitw -h
bind - splitw -v
set -g status-bg black
set -g status-fg yellow
set -g pane-active-border-fg white
set -g pane-active-border-bg yellow
setw -g window-status-fg blue
#setw -g window-status-attr dim
setw -g window-status-current-fg white
setw -g window-status-current-attr bright,underscore
set -g status-justify centre
set -g status-right "#[fg=green][#(date +%x%H:%M)]#[default]•#[fg=green][#(cut -d ' ' -f 1-3 /proc/loadavg)]#[default]"
#set -s escape-time 0
bind r source-file ~/.tmux.conf
